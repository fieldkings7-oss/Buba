<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/shop_bg">

    <!-- Кнопка возврата в меню (крестик) -->
    <ImageButton
        android:id="@+id/button_back_to_menu"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="top|right"
        android:layout_margin="20dp"
        android:background="@android:drawable/ic_menu_close_clear_cancel"
        android:contentDescription="Back"
        android:scaleType="centerInside"
        android:tint="@android:color/white" />

    <!-- Основной контент -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_gravity="center"
        android:padding="40dp">

        <!-- Заголовок -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Выберите карту"
            android:textSize="48sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:layout_marginBottom="80dp"
            android:shadowColor="@android:color/black"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="4" />

        <!-- Default Map Button -->
        <Button
            android:id="@+id/button_default_map"
            android:layout_width="350dp"
            android:layout_height="100dp"
            android:text="🌿 Default Map"
            android:textSize="28sp"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:background="#3F51B5"
            android:layout_marginBottom="40dp"
            android:shadowColor="@android:color/black"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="4" />

        <!-- Prepared Yard Button -->
        <Button
            android:id="@+id/button_prepared_yard"
            android:layout_width="350dp"
            android:layout_height="100dp"
            android:text="🏡 Prepared Yard\n(50 монет)"
            android:textSize="24sp"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:background="#4CAF50"
            android:shadowColor="@android:color/black"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="4" />

    </LinearLayout>

</FrameLayout>
